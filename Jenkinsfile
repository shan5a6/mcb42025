pipeline {
  agent any 
  stages {
    stage('working with variables') {
      steps {
        script {
          age = 30
          subject = "devops"
          println "age is: ${age} & subject is subject: ${subject}"
        }        
      }
    }
  }
}
